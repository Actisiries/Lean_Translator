# Core (already available in many environments)
pdfplumber>=0.11.0
pypdf>=4.0.0
numpy>=1.26.0

# Optional – install when internet / keys are available
# openai>=1.0.0
# anthropic>=0.30.0
# chromadb>=0.5.0
# sentence-transformers>=3.0.0
# scikit-learn>=1.4.0   # for better TF-IDF / cosine if desired

# Lean verification requires a local Lean 4 + Mathlib installation
# (not a Python package). See https://lean-lang.org
